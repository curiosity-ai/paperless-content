<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <RootNamespace>Paperless.Content.OnnxParity</RootNamespace>
    <AssemblyName>paperless-content-onnx-parity</AssemblyName>
    <ServerGarbageCollection>true</ServerGarbageCollection>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/Paperless.Content/Paperless.Content.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The harness drives internals directly: it must see individual node outputs, which
         are not part of the package's public surface. -->
    <Using Include="Paperless.Content.Internal.Onnx" />
  </ItemGroup>

</Project>
